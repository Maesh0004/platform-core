[defaults]
# Example ansible config (sanitized)
# Use this file as a template. Copy to `ansible/ansible.cfg` and adjust paths.
inventory = inventory.ini
host_key_checking = False
# private_key_file: set to your SSH key path, do not commit private keys
private_key_file = ~/.ssh/my-keypair.pem
ansible_user = ubuntu
